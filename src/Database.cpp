#include "Database.h"

using namespace std;

Database::Database() {}

// TODO: Which database to use?
bool Database::checkLogin(const string& username, const string& password) {
	if (username.length() > 0 && password.length() > 0) {}
	
	return true;
}